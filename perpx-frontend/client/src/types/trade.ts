/**
 * ============================================================
 * Trade Type
 * ============================================================
 */

export type Trade = {
  id: string;
  symbol: string;
  price: number;
  pnl: number;
  timestamp: number;
};
